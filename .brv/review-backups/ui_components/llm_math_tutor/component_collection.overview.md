- **Key Points**
    - Documents a collection of 12 React components located in the `src/components` directory.
    - Components serve various functions: core UI (`Header`, `Footer`), chat interface (`AITutorChat`), math rendering (`MathEquation`, `MathMarkdownRenderer`), and SEO (`SeoBlocks`).
    - Highlights several specialized, interactive learning modules: `SigmoidLogitModule`, `SoftmaxGradientModule`, and `TemperatureSimulator`.
    - Identifies `KaTeX` as a key dependency for rendering mathematical formulas.
    - Provides a complete list of all 12 component file paths in the "Facts" section.

- **Structure / Sections Summary**
    - **Reason:** States the document's purpose is to curate an overview of the project's components.
    - **Raw Concept:** Lists all 12 component files and describes a high-level user interaction flow.
    - **Narrative:** Provides a descriptive summary of the `src/components` directory, grouping components by their functionality (UI, math, interactive modules, SEO).
    - **Facts:** Contains a structured list of `component_location` facts, mapping each component name to its exact file path.

- **Notable Entities, Patterns, or Decisions**
    - **Entities:** `AITutorChat`, `MathMarkdownRenderer`, `SigmoidLogitModule`, `SoftmaxGradientModule`, `TemperatureSimulator`.
    - **Pattern:** A modular, component-based architecture common in React applications, promoting reusability and separation of concerns.
    - **Decision:** The use of the `KaTeX` library for efficient and high-quality client-side math rendering.
    - **Decision:** The creation of dedicated interactive components (`SigmoidLogitModule`, etc.) to provide hands-on learning experiences for specific mathematical concepts.