### Key Points
- This document outlines the utility functions available in the frontend.
- The utilities are primarily focused on mathematical calculations.
- Functions are centralized in the `src/utils` directory.
- Specific examples mentioned are `softmax`, `sigmoid`, and `logit`.

### Structure / Sections Summary
- **Reason**: To document frontend utility functions.
- **Raw Concept**: States the task and identifies the relevant file, `src/utils/math.ts`.
- **Narrative**:
    - **Structure**: Specifies that utility functions are located in the `src/utils` directory.
    - **Highlights**: Details the contents of `src/utils/math.ts`, which contains mathematical calculation helpers.

### Notable Entities & Decisions
- **Architectural Pattern**: Centralizing reusable functions in a `utils` directory is a standard practice for code organization and avoiding duplication.
- **Entities**:
    - `src/utils/math.ts`: The sole file mentioned, containing math-related helper functions.
- **Functions**: `softmax`, `sigmoid`, and `logit` are highlighted as key mathematical utilities provided.